; For PHP-FPM/FastCGI hosts (common on shared hosting control panels), where
; .htaccess php_flag directives are ignored. See .htaccess for why this is
; needed: it lets upload.php read the raw multipart body itself so every
; uploaded file survives, not just the last one.
enable_post_data_reading = 0
